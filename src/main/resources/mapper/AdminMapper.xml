<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="aftnos.aftourismserver.auth.mapper.AdminMapper">

    <!-- 查询列封装，避免重复书写 -->
    <sql id="Base_Column_List">
        id, username, password, real_name, phone, email,
        avatar, introduction, role_code, is_super, status, remark,
        is_deleted, create_time, update_time
    </sql>

    <!-- 根据账号查询管理员 -->
    <select id="findByUsername" resultType="aftnos.aftourismserver.auth.pojo.Admin">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_admin
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- 根据主键查询管理员 -->
    <select id="findById" resultType="aftnos.aftourismserver.auth.pojo.Admin">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_admin
        WHERE id = #{id}
        LIMIT 1
    </select>

    <!-- 条件分页查询管理员 -->
    <select id="search" resultType="aftnos.aftourismserver.auth.pojo.Admin">
        SELECT
        <include refid="Base_Column_List"/>
        FROM t_admin
        WHERE is_deleted = 0
        <if test="username != null and username != ''">
            AND username LIKE CONCAT('%', #{username}, '%')
        </if>
        <if test="realName != null and realName != ''">
            AND real_name LIKE CONCAT('%', #{realName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY update_time DESC
    </select>

    <!-- 新增管理员 -->
    <insert id="insert" parameterType="aftnos.aftourismserver.auth.pojo.Admin" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_admin (
            username, password, real_name, phone, email,
            avatar, introduction, role_code, is_super, status, remark
        ) VALUES (
            #{username}, #{password}, #{realName}, #{phone}, #{email},
            #{avatar}, #{introduction}, #{roleCode}, #{isSuper}, #{status}, #{remark}
        )
    </insert>

    <!-- 更新管理员 -->
    <update id="update" parameterType="aftnos.aftourismserver.auth.pojo.Admin">
        UPDATE t_admin
        <set>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="introduction != null">introduction = #{introduction},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="roleCode != null">role_code = #{roleCode},</if>
            <if test="isSuper != null">is_super = #{isSuper},</if>
            <if test="status != null">status = #{status},</if>
            <if test="password != null">password = #{password},</if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <!-- 更新管理员状态 -->
    <update id="updateStatus">
        UPDATE t_admin
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 逻辑删除管理员 -->
    <update id="softDelete">
        UPDATE t_admin
        SET is_deleted = 1, update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 查询管理员表中出现的全部角色编码（逗号分隔） -->
    <select id="findAllRoleCodes" resultType="string">
        SELECT DISTINCT role_code FROM t_admin WHERE is_deleted = 0
    </select>

</mapper>
